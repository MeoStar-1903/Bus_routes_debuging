# Copy thành .env và điền giá trị (dùng cho docker-compose hoặc docker run --env-file)
# Không commit file .env lên git.

PORT=3000
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=your_strong_password_here
DB_NAME=busdb
DB_PORT=5432
JWT_SECRET=your-jwt-secret-change-in-production
